git checkout styling

git add .

git commit -m "tambah file product"

git push origin styling

<!-- diatas buat ngepush ke github -->

<!-- dibawah buat fetch update dari github -->

git pull origin styling